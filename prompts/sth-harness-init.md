---
description: Install the ai-harness into this repo, then interview to fill project.md and the first spec.
argument-hint: "[feature-name]"
---

Use the `sth-harness` skill and run its Init workflow.

If an argument was provided, treat it as the proposed first feature name. Ask one
question at a time, do not invent requirements, and use the bundled scripts and
template files from the installed package.
