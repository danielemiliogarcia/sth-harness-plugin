---
description: Pick the next pending ai-harness spec and implement it until blocked, done, or a decision is needed.
---

Use the `sth-harness` skill and run its Implement Next Spec workflow.

Rank pending specs from their `state.md` files, ask for confirmation when the
workflow requires it, and update the chosen feature's `state.md` before stopping.
