---
description: Interview for a new feature and create its ai-harness spec.md + state.md.
argument-hint: "[feature-name]"
---

Use the `sth-harness` skill and run its Add Spec workflow.

If an argument was provided, treat it as the proposed feature name. Ask one
question at a time, write only inside `ai-harness/specs/<feature>/`, and keep the
harness reference files as the source of truth.
